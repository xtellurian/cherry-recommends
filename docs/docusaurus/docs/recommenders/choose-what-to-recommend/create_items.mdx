---
sidebar_position: 10
title: Create Recommendable items
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";


Your Recommender needs Items to send to given customers. 

## In Web application

First, go to `Library` in the header and select `Items`.

You now should see a catalogue of all recommendable items. Click `Create Recommendable Item` in the top-right.

Now fill out the details of the item. The `Item Identifier` should be a name that works in your stack can could be a `Discount Code`, `SKU Id`, or `Plan Id`.

Add in the `Price` and your `Cost` of the `Item`. 

Press `Create` and you're done!

![Create Item](/img/screenshots/recommend/create_item.png)

## Through API
You can create an item programatically with our API.

You need to submit the payload 

<Tabs
  defaultValue="js"
  values={[
    { label: 'JavaScript', value: 'js', }
  ]
}>

<TabItem value="js">

```js
import { events } from "signalbox.js";
const ACCESS_TOKEN = "your access token";

// the recommendation comes from invoking the recommender
events
  .createItemAsync({
    "commonId": "string",
    "name": "string",
    "listPrice": 0,
    "directCost": 0,
    "description": "string",
    "properties": 
    {
        "property1": null,
        "property2": null
    }
    })

```

</TabItem>
</Tabs>



