import { products } from "signalbox.js";

export const fetchProducts = products.fetchProducts;
export const fetchProduct = products.fetchProduct;
export const createProduct = products.createProduct;
export const deleteProduct = products.deleteProduct;
