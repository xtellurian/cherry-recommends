import { segments } from "cherry.ai";

export const fetchSegmentsAsync = segments.fetchSegmentsAsync;

export const fetchSegmentAsync = segments.fetchSegmentAsync;

export const createSegmentAsync = segments.createSegmentAsync;

export const deleteSegmentAsync = segments.deleteSegmentAsync;

export const addCustomerAsync = segments.addCustomerAsync;

export const removeCustomerAsync = segments.removeCustomerAsync;

export const fetchSegmentCustomersAsync = segments.fetchSegmentCustomersAsync;
